name: workify
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^5.0.0
  http: ^0.13.3
  dio: ^4.0.0
  json_serializable: ^4.1.3
  flutter_svg: ^0.22.0

  #ML
  google_ml_kit: ^0.6.0

  #Device
  camera: ^0.8.1+3

  #Possible solutions
  #openfoodfacts: ^0.3.15+3 for open food facts api

  #UI
  material_floating_search_bar: ^0.3.4
  salomon_bottom_bar: ^3.0.0
  line_icons: ^2.0.1
  flutter_speed_dial: ^3.0.5
  textfield_tags: ^1.3.1
  timelines: ^0.1.0
  google_fonts: ^2.1.0
  chart_sparkline: ^1.0.5

  #state/flow managment
  carousel_slider: ^4.0.0

  #Firebase
  firebase_core: ^1.3.0
  firebase_database: ^7.1.1
  firebase_auth: ^1.4.1
  cloud_firestore: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  stacked_generator: ^0.4.1

  places_service: ^0.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/AppLogo/
    - assets/images/StudioLogo/

 
  
  
  
  

  
  

  
  

  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
